{
  "name": "dds",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --filter @dds-wg/website dev",
    "build": "pnpm --filter @dds-wg/website build",
    "preview": "pnpm --filter @dds-wg/website preview",
    "lint": "pnpm --filter @dds-wg/website lint",
    "format": "pnpm --filter @dds-wg/website format"
  }
}
