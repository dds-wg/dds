// Tailwind v4 is configured via the CSS @theme block, not a JS preset. This file is
// retained as a future hook for any non-CSS-expressible config (plugins, content globs)
// the apps should share.
export default {};
