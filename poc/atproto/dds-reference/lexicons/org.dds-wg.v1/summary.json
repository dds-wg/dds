{
  "lexicon": 1,
  "id": "org.dds-wg.v1.summary",
  "description": "A textual summary of a subject (typically a group or decision).",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name", "description", "creator", "subject", "createdAt"],
        "properties": {
          "schemaVersion": { "type": "string", "maxLength": 32 },
          "name": { "type": "string", "maxLength": 200 },
          "description": { "type": "string", "maxLength": 10000 },
          "creator": {
            "type": "union",
            "refs": ["org.dds-wg.v1.defs#userAgent", "org.dds-wg.v1.defs#algorithmAgent"]
          },
          "subject": { "type": "ref", "ref": "com.atproto.repo.strongRef" },
          "step": { "type": "ref", "ref": "org.dds-wg.v1.defs#projectStepRef" },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
